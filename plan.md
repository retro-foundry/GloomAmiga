# Gloom PC Port Plan (Updated)

## Scope
- Keep all legacy Amiga code and assets under amiga/ as immutable source material.
- Build a new PC runtime in C17 with SDL2, fetched from source via CMake FetchContent.
- Start with parity foundations: deterministic fixed-step update, map parser, and bootable window loop, preserving Amiga
  25 Hz gameplay-tick motion rates where the PC runtime uses a different logic clock.
- Gameplay work is a port, not a redesign: do not add speculative mechanics or convenience AI. Every enemy/combat behavior must be traceable to `amiga/gloom2.s` or another original asset/source file; unported behavior should remain absent or be marked TODO instead of filled in.

## Completed
- Legacy repository content moved under amiga/.
- Map header endianness validated on amiga/maps/map1_1 (big-endian 32-bit offsets).
- Initial zone record layout validated from real bytes (32-byte zone records).
- Root CMake bootstrap created with SDL2 fetched from source via FetchContent (no SDL binaries).
- New C17 codebase scaffolded in src/ with map parser and fixed-step SDL app loop.
- Full configure + Debug build succeeds and produces build/Debug/gloom_pc.exe.
- SDL window now renders map-derived zone wireframe debug view from parsed map geometry.
- Sweep mode added (`--sweep`) to validate map parsing across directories.
- Sweep validated current parser coverage: 42 raw maps parsed, 42 CrM2 container maps detected and skipped.
- IFF parser module added for FORM/ILBM metadata (`BMHD`/`CMAP`/`BODY`) with CLI inspection mode (`--iff-info`).
- IFF parser validated on amiga/combat.iff (320x240, 7 planes, 128-color palette, compressed BODY).
- ILBM BODY decode implemented (ByteRun1 + planar-to-indexed conversion for modes masking=0/1/2).
- SDL IFF preview mode added (`--iff-preview`) and validated on amiga/combat.iff.
- Main map debug window now includes an integrated decoded texture panel (map + texture visible together).
- First software wall debug pass added to main window (camera orbit + projected zone walls + texture sampling).
- Software wall panel upgraded with horizon/floor depth shading and first billboard sprite/debug-object pass.
- Wall pass now clips segments against side frustum planes to reduce projection artifacts.
- Sprite pass now depth-sorts billboards and applies simple frame stepping for visible animation.
- CrM2 map container decompression implemented from the original Crunch-Mania LZH decruncher semantics.
- Sweep now validates both raw and compressed map directories: 84 maps parsed, 0 skipped, 0 failed.
- Map parser now exposes the real 32x32 wall/trigger grid cells from the map header grid block.
- Wall debug pass now uses the original `gridoffs4.bin` search order plus grid/ppnt wall candidates instead of drawing every zone segment.
- Main viewport now starts from the event-1 player spawn and renders full-screen wall columns from real map grid candidates using the zones' real texture slot/scale data.
- Wall texture coordinate selection now follows the Amiga wall renderer's half-range `wl_sc` path: `wl_sc=2` maps one full texture across the wall, `wl_sc=4` advances to the second texture slot halfway along, and non-positive scales preserve the original doubled-then-shifted coordinate shape.
- Main runtime now treats missing named texture screens or zone references to unloaded texture screens as fatal startup errors.
- Main viewport now derives the map's `tile_` tag from the original script and renders perspective floor/roof flats from the real `txts/floor*` and `txts/roof*` tile assets.
- Event parsing now preserves real open-door and texture-change commands, validates command texture references, and the runtime triggers events from the map trigger grid using the player collision radius read from the real `objs/player` asset. Trigger/switch activation now follows the Amiga `checkevent`/`checknew2` shape: touch-based, no separate use/fire input, first colliding trigger in grid order only, and cleared negative triggers still block later overlapping triggers.
- Door events now animate the target wall geometry and `zo_open` word in the fixed update path, including the Amiga wall-column open-threshold scale and fixed-point endpoint slide behavior.
- Wall texture loading now keeps the original per-column transparency byte and skips transparent texels for see-through wall strips instead of treating every 65-byte texture column as opaque.
- Wall rendering now gathers multiple ray hits per column and draws them front-to-back per pixel, so transparent wall texels reveal farther real walls/flats instead of terminating the ray at the nearest strip.
- Object spawns now bind through the original object type table to real external `objs/*` animation assets, decode their packed Amiga palettes and column-major frames, and render supported map objects with their source handles, scales, and rotation frames instead of the old `rot*.iff` debug smoke pass.
- Embedded weapon pickup object types now resolve to the original `bullet1.bin` through `bullet5.bin` animation payloads, so first-map weapon spawns render from the real binary include data instead of being skipped.
- Renderer draw distance now uses the original Amiga `maxz` cutoff (`16 << darkshft` = 2048 view-Z units) for floors, walls, and objects, with the reversed `initdarktable` depth lookup and 4-bit per-channel palette subtraction so the far band fades to black before culling.
- Floor/roof tile sampling now matches the Amiga flat renderer's column-major address order `((world_x & 127) << 7) + (world_z & 127)` and uses the same fixed `focshft=6` projection scale as walls, fixing flat scroll/parallax against wall movement.
- Event parsing and runtime execution now preserve teleport and rotpoly commands; teleport events use the original delayed pixel-out timing before moving the player, and rotating/morphing polygon events mutate live zone geometry using the Amiga `dorots`/`exec_rotpolys` data model.
- Animated wall textures now follow the original `doanims` model: the runtime keeps a live 160-entry `textures` remap table, advances map animation countdowns on Amiga ticks, rotates the global texture entries, and applies the remap in both fast and fallback wall sampling paths instead of inventing per-zone texture cycling.
- Player wall collision now follows the Amiga `checknewslow`/`adjustpos` path: movement scans the wall grid plus active rotpolys, uses the real player radius from `objs/player`, pushes along the nearest wall normal twice, and reverts/stalls when unresolved.
- Player control now uses the Amiga `rotplayer`/`moveplayer` constants for rotation acceleration, movement speed, and movement bob, while adding modern relative mouse look and WASD strafe/forward input on the same player state.
- Widescreen rendering now adapts the viewport width to the renderer shape, scales projection focal from the Amiga large-window 106x80 chixel/`focshft=6` reference so FOV matches the original instead of the full 320-pixel display, and keeps `--classic` for regression checks.
- The SDL runtime now runs gameplay on a fixed 60 Hz step with Amiga 25 Hz gameplay-tick motion/timer rates scaled for parity, renders uncapped, interpolates camera position/bob and live door/rotpoly zone geometry between logic steps, and applies mouse orientation every render frame.
- Player firing now follows the Amiga `checkfire` one-shot/reload cadence, spawns live runtime projectiles from the original `wtable` weapon speeds/damage values, and draws bullets and wall-hit sparks from the real `bullet*.bin`/`sparks*.bin` payloads.
- The Gloom Deluxe ADFs have been extracted under `build/adf_extract`; Disk 1 contains the missing real on-screen weapon payload `misc/gun.bin`, which is now copied into `amiga/misc/gun.bin`, decoded to `amiga/art/objects/misc__gun.bin`, and rendered as the player gun instead of inventing or scaling projectile art.
- The on-screen gun now uses the real `misc/gun.bin` frames for idle/recoil and the original red/orange, green, and purple muzzle flash variants, draws at integer logical-pixel coordinates, renders the muzzle flash behind the gun body, applies recoil while firing, matches the flash colour to the represented `wtable` shot art (`bullet1` red/orange, `bullet2`/`bullet3` green, `bullet4`/`bullet5` purple), and bobs in the slight smile-shaped left/right footstep cycle.
- Projectile spawning now defaults to a centered barrel-origin start point 2.5 projectile radii in front of the player, while preserving the legacy player-position path through `--player-projectiles`/`--legacy-projectiles` so gameplay changes remain testable; owner-view projectile rendering now waits until shots are at least three projectile radii from the player and pads the larger non-red `bullet*.bin` billboards with transparent PC-side source margin so their original edge-touching art is not clipped in the near view.
- Barrel-origin projectiles carry their original player-origin coordinates too, ready for the future enemy-hit compatibility rule where a very close center-view enemy should still count if it would have been hit by the old start position.
- Runtime enemy combat state now mirrors the Amiga object table hitpoints, damage, movement speed, `ob_base`/`ob_range`, `ob_frame`, `ob_framespeed`, and `ob_hurtpause` fields for represented monsters; player projectiles sweep-test against live enemies before wall hits, and killed enemies are removed from sprite rendering.
- Runtime enemies now keep live position/facing state, use the generic Amiga `monsterlogic` movement/fire timing for object type 10, use `hurtobject`/`pauselogic2` hurt frame/pause timing from `ob_hurtpause`, and expose player health in the window title while broader player death flow remains future work.
- Enemy-owned projectiles now exist for the generic `monsterlogic`/`fire1` path using the Amiga bullet1 parameters; enemy bullets damage the player instead of monsters, and player HP now enters a dead state that freezes control and marks the window title as `DEAD`. Special monster firing routines are now represented separately from their original routines instead of sharing invented generic projectile behavior.
- Enemy movement cleanup removed non-Amiga line-of-sight/range gates, artificial pack separation, and placeholder deathhead charge behavior so future monster work stays tied to direct `gloom2.s` routine ports rather than invented substitutes.
- Generic enemy movement now follows the `monsterlogic`/`monsterfix` shape more directly: monsters move along their own `ob_rot`, tick an `ob_delay`-style timer initialized from `ob_base`/`ob_range`, turn to the player only for `fire1`, pause for 7 ticks after firing, and use the Amiga wall-hit turn sequence (+/-90, 180, oldrot+180) instead of direct chase steering.
- Generic enemy movement/collision now follows `checkvecs` more closely: blocked proposed movement reports a wall hit instead of using player-style wall push/slide, `fire1` applies its original `(rndw & 31)-16` aim noise, and `pauselogic` may restore `ob_oldrot` after firing using the original player-facing angle test.
- Baldy-family melee behavior now ports the shared `baldylogic`/`bl2`/`baldycharge`/`baldypunch` shape used by baldy, lizard, and troll: `ob_delay` gates charge, charge uses `ob_movspeed` and `ob_framespeed` multiplied by four, wall/angle loss returns through `monsterfix` plus `rnddelay`, and collision-gated punch frames apply `ob_damage` on the original `ob_punchrate` cadence.
- Runtime enemy/player contact now follows the represented Amiga `obj_loop`/`checkcoll`/`ob_washit` path for active enemies: player bits are tested in player-one then player-two order, the first new overlapping pair applies both the enemy's `ob_damage` to the player and the player's table `ob_damage=1` back to the enemy, repeated same-pair overlap is suppressed until separation, `finished2` blocks contact damage, and baldy-family punch frames re-arm contact by clearing the represented `ob_washit` marker instead of applying direct PC-side damage.
- Deathhead cruise/charge movement now ports `deathheadlogic`, `deathcharge`, `deathbounce`, and `deathanim`: `ob_delay` is preserved as signed rotation speed, wall hits reverse and reseed through `rnddelay`, near-facing deathheads enter charge, charge exits on wall hit or wide angle, and the frame oscillator stays in the original `$8000..$28000` range.
- Special projectile monster logic now ports `terralogic`/`terralogic2`, `ghoullogic`, `phantomlogic`, and `demonlogic`/`demonpause`: terra fires five bullet4 shots on `ob_firerate`, ghoul bobs/fires bullet2 and ignores walls, phantom fires bullet3 then enters the original 7-tick pause, and demon fires the five-weapon `wtable` burst with the original 3/4 damage scaling.
- Dragon movement/firing now ports `dragonlogic`, `dragonanim`, `dragonfire`, `getobrot`, and the represented part of `homeinlogic`: dragon rotation uses `ob_rotspeed`, fire timing emits bullet5 homing projectiles during the negative `ob_delay` burst window, and homing bullets accelerate toward the player with the original per-axis speed cap.
- Dragon death now ports the represented `blowdragon`/`dragondead` shape: 64 `bloodymess2` splots, four `blowchunx` passes from `dragon2`, render/collision removal, and the original 127-tick death wait before setting `finished=3` for the script-driven end-game path.
- Deathhead death dispatch now preserves `blowdeath`: if the unported soul-suck owner state is not active it follows through to `blowobject`, matching the original non-sucking path instead of pretending the deathhead has a generic table death.
- Deathhead non-lethal hit behavior now ports `hurtdeath`, `deathsuck`, and `addsoul` using the original global `sucking`/`sucker` shape: one active deathhead suck owner, 64-tick suck duration, `deathbounce`/`deathanim` during the state, rotation toward the player each tick, four soul-colored blood-list particles per tick, and return through `rnddelay` when the suck ends.
- Terra death dispatch now keeps `blowterra` as its own `objinfo` death routine and routes through the original `blowquick` body after the still-unported `robodiesfx` audio call, instead of collapsing the table entry to generic `blowquick`.
- Focused combat parity selftest mode (`--combat-selftest`) now checks the original `wtable` weapon hitpoints/damage/speed values, representative `objinfo` enemy health/damage/death routine entries, `demonpause` 3/4 `wtable` damage scaling, the `hurtdeath`/`deathsuck`/`addsoul` state transition plus soul particle count, projectile cadence/parameters for `terralogic2`, `phantomlogic`, `demonpause`, and `dragonfire`, and the represented two-player combat projectile/death `finished=4` path.
- Enemy hurt handling now follows `hurtobject`/`pauselogic2` for the table-driven `ob_hurtpause` wait and frame 4 hurt pose, then clears back to frame 0 when the hurt pause completes.
- Enemy hit reaction rendering now matches `drawshape_8`: the renderer no longer masks `ob_frame` to frames 0-3, so `hurtobject` frame 4 is visible while walking animation still wraps in the movement logic.
- Enemy non-lethal hit dispatch now follows the represented `objinfo` hit routines instead of treating every monster as generic `hurtobject`: dragon hits use `rts`, ghoul hits use `hurtghoul` blood-only behavior, generic grunts/lizard/troll/terra still use `hurtobject`, and deathhead hits use the ported `hurtdeath`/`deathsuck` path.
- Enemy blood now uses a `maxblood`-sized runtime list ported from `bloodymess`, `bloodymess2`, `bloodspeed`, `bloodspeed2`, `bloodspeed3`, `moveblood`, and `drawblood`: non-lethal hits emit 24 blood splots with `bloodspeed`, generic deaths emit 32 splots with `bloodspeed3`, gravity is `#$8000` per Amiga tick, and pixels are shaded with `blcols & ob_blood`.
- Generic enemy deaths now load the original `ob_chunks` payloads from `amiga/objs/*2` and port `blowchunx` plus the default `mode=0` `chunklogic2` path: each chunk frame is spawned from `bloodspeed3`, starts at y=-64 with y velocity minus `#$40000`, receives `#$8000` gravity per Amiga tick, renders through `drawshape_1sc`, and is killed when it reaches the floor.
- The original violence model switch is represented with `--meaty` for `mode=0` chunk vanish behavior and `--messy` for `mode=1` persistent `maxgore` plus wall-stop behavior. The PC runtime now defaults to an explicitly user-requested `MEATY+MESSY` mode (`--meaty-messy`/`--gore`) that keeps the freer MEATY chunk motion while also persisting chunks as MESSY ground gore.
- Persistent gore size now follows the original render split: flying chunks use `drawshape_1sc` with the dying object's `ob_scale`, while floor gore uses `drawshape_q` at fixed `$200` scale.
- SDL SFX playback now loads the original Paula one-shot assets from `amiga/sfxs/*.bin` using the `playsfxnow` period/word-length header format, mixes four Amiga-style priority channels with Paula channel panning, and hooks represented calls for `checkfire`, `footstep` from `moveplayer`/`unbounce`, `splat`, `blowobject`, `blowterra`, `blowdragon`, `hurtngrunt`/`hurtterra`/`lizhurt`/`trollhurt`, `terralogic`, `terralogic2`, `ghoullogic`, `demonpause`, `dragonlogic`, door events, and teleports.
- SDL SFX channel lifetime now keeps the original `playsfx`/`sfxvbint` four-channel first-free and priority-steal queue shape, but the SDL mixer frees one-shot channels immediately after the represented sample finishes instead of reserving a silent full-sample tail; this avoids PC-side channel starvation while preserving non-looping playback, channel panning, and next-tick starts for stolen queued sounds.
- Player pickups now port the represented `healthgot`, `weapongot`/`weapond0`, `thermogot`, `invisigot`, `invincgot`, `bouncygot`, `playertimers`, `weaponlogic`, and the player `shoot` mega/bouncy paths: token SFX plays from `playtsfx`, health adds 5 up to 25, repeated weapon pickups reduce `ob_reload` and then add 250 `ob_mega` ticks, thermo/invisibility add 1500 Amiga ticks, hyper starts at `#-$200` and advances on the Amiga tick scale, bouncy bullets cap at 3, consumed pickups disappear, weapon pickups bob/animate with the original `camrots` negative-up Y range without overwriting map-authored pickup placement, weapon projectile hitpoints/damage/speed stay tied to `wtable`, mega/ultra-mega fire the original two/three-shot spreads, and bouncy projectiles reflect from wall normals while their bounce count remains.
- Player injury/death visual feedback now ports the represented `redpal`/`ob_paltimer` path: player damage sets a two-Amiga-tick red palette timer, `playertimers` counts it down, and the PC framebuffer applies the original red-only `palettesr` brightness transform while active.
- Player one-player death/restart flow now ports the represented `playerdie`, `playerdeath`, `playerdead`, `waitrestart`, `playerlogic0`, and unresolved `playerlogic` push/squash injury path: lethal damage starts the red-flash death logic without consuming a life immediately, the view rotates and the eye height rises to the original `ob_eyey=-32` clamp before subtracting one life, `playerdead` waits 63 Amiga ticks, `waitrestart` requires a fresh fire press, respawn restores the event-1 spawn position/rotation, preserves only `ob_weapon`, clears `resetplayer` powerup state (`ob_mega`, `ob_thermo`, `ob_infra`, `ob_invisible`, `ob_bouncecnt`, pixel state), and `playerlogic0` gives 75 Amiga ticks of no-damage invulnerability before normal collision resumes. Final one-player life loss sets `finished=2`; the represented combat death branch subtracts one loser life and sets `finished=4` for the combat round flow.
- Cooperative two-player mode is now exposed with `--two-player`/`--2p`: the runtime initializes a second player from the next original player object spawn when present, runs player two through the same represented player logic/death/restart/pickup/weapon paths by swapping it into the active player object, renders the PC port as a vertical left/right split down the middle, ports the represented cooperative shared-life branch from `playerdeath`/`playerdead` where nonzero remaining lives are mirrored and a zero-life player drops out unless both players are out, and follows the original `pickplayer` nearest-live-player targeting for enemy aiming/contact/projectile hits. Player-two PC input is mapped to I/K, J/L, U/O, and Enter/Right Ctrl as a modern runtime control shim outside core gameplay parity; SDL gamepads are also isolated as PC input shims, with menu-selectable unique player controls defaulting to player one keyboard and player two joystick 1, two-joystick selection normalized to player one joystick 1/player two joystick 2, and keyboard falling through to player one when neither player selected keyboard.
- Teleport and exit trigger handling now ports the represented `exec_teleport`, `dotelesfx`, `checkevent`, `playertimers`, `levelover`, `scriptplay`, `execscript`, and `scriptdone` level/game-completion path: ordinary teleport commands store the original target words, start `ob_pixsizeadd=2`, move/rotate the player when scaled `ob_pixsize` reaches 24, reverse through pixel-in to zero, and play `telesfx` at priority 10; event id 24 starts the original `finished2=3` level-complete pixel-out, presents the repo-local Amiga `pixelate` block-copy style visible transition from `amiga/gloom.s`/`amiga/ap.s` before the load, preserves both players' represented health/lives/weapon/reload state through the new map's player reset, applies the cooperative `levelover` shared-lives max before `scriptplay`, reads the next `play_` map from `misc/script`, loads the next map/assets, and handles terminal `pict_`/`text_`/`done_` completion by showing the original script picture/text and returning through the startup menu.
- Scriptplay map progression now prefers the packaged `amiga/data/maps` assets for `misc/script` `play_` commands, matching the Amiga packaged `maps/<name>` load path instead of stale raw source-map duplicates. Level transition loading also represents the original `freetxts`/`loadtxts` stale texture table behavior by preserving previous texture-table slots that a later map still references but does not reload; the transition selftest now walks the full original play-chain through `map4_7` and loads every packaged combat map from `com1_1` through `com3_7`.
- Bottom status display now decodes the original `gloom2.s` chunky panel asset `misc/smallfont2.bin` using the `drawchunky` table layout at `12 + shape*4`, then draws only the user-requested non-solid HUD contents at the original `showstats` coordinates: health draws repeated shape 45 pips from x=267, lives draws shape 44 face icons from the original `218 + ((6 - ob_lives) * 8)` right-aligned life positions without a PC-side six-life cap, and weapon readiness draws `ob_weapon+39` chunks from x=55. Shape 47 base panel and shape 46 status strip are intentionally not rendered in the PC HUD; the earlier invented text labels were removed, runtime lives initialize to 3 and decrement once on player death, and PC HUD/gun presentation scales by exact 1x/2x/4x/8x pixel duplication to avoid fractional UI distortion.
- Deterministic replay selftest harness added: the fixed-step simulation can now be driven from explicit `joyx`/`joyy`/`joys`/`fire` controls matching the represented `rotplayer`/`moveplayer` input fields, `--replay-selftest [map] [replay.txt]` runs a replay twice without a window and compares a gameplay-state fingerprint, and optional text replay rows use `ticks joyx joyy joys fire [mouse_dx]`.
- PC renderer transport made efficient without changing the current Amiga-derived sampling/draw order: floors, walls, sprites, blood, gun, and HUD now write into a persistent ARGB framebuffer that is uploaded once per frame through a streaming SDL texture, and the pixelate transition reuses that texture instead of creating a render target every frame.
- Renderer scratch buffers for wall depth, sprite depth, and per-column transparency coverage are now persistent alongside the PC framebuffer instead of allocated every frame; hot sprite/gun drawing paths write directly into the framebuffer after their existing clipping, and the main SDL renderer no longer requests target-texture support that the runtime no longer uses.
- Runtime render/interpolation memory is now deterministic at startup: previous/current zone interpolation buffers are fixed `GLOOM_MAX_RENDER_ZONES` allocations reused across level transitions, over-capacity maps fail loudly, and the PC framebuffer/scratch texture is created before the main loop instead of first render.
- The core render routine is now allocation-free: framebuffer preparation happens at startup or explicit viewport-size changes, while `render()` only clears, draws into existing memory, uploads, and presents.
- Pixel plotting now targets locked SDL streaming texture memory directly using the texture pitch, eliminating the extra CPU framebuffer-to-texture upload copy; `--render-scale 1..6` deliberately raises internal logical resolution while preserving the current projection and draw-order rules.
- Runtime internal renderer resolution now defaults to a 720p-height preset chosen from the display aspect ratio, mirroring the AB3D web aspect bucket shape: 16:9 uses 1280x720, 16:10 uses 1152x720, 3:2 uses 1080x720, 4:3 uses 960x720, 21:9 uses 1680x720, and 32:9 uses 2560x720. The startup window is created at the same size; `--window-size WIDTHxHEIGHT`/`--resolution WIDTHxHEIGHT`/`--boot-resolution WIDTHxHEIGHT` set both the internal render target and initial window size, while later user window resizes only scale the fixed render target.
- 720p CPU-renderer hot paths optimized without changing current sampling rules: Amiga depth darkening now uses a precomputed table instead of per-pixel square roots, floor/roof rows step world coordinates incrementally, wall spans precompute texture-column lookup and depth subtraction outside vertical loops, and ray columns skip walls outside conservative projected column bounds.
- 720p palette paths tightened further: wall and flat texture loaders now precompute the 16 Amiga depth-shaded palettes, hot floor/wall pixels use direct shaded palette-index lookup instead of RGB subtract/clamp, and wall transparency coverage uses per-column stamps instead of clearing a height-sized buffer for every ray column.
- DOS performance pass now follows the FastDoom-style low-bus-pressure shape without changing Gloom fidelity: `GLOOM_DOS_SDL3` defaults to the lowest full-width 320x200 classic VGA viewport, keeps `--widescreen` at 400x200 unless an explicit `--resolution` is supplied, samples detailed DOS wall profiling every 16 columns to keep profile runs close to real frame cost, measures SDL/DOS event-pump cost separately, draws indexed flats before walls so the flat pass no longer pays wall-coverage checks or allocates the old coverage buffer, pre-shades original floor/roof texels and opaque wall columns into DOS index buffers, addresses 128x128 flat texels directly from fixed-point masks through an unrolled row helper, caches each ray-hit texture column/transparency lookup while preserving the front-to-back transparent wall reveal rule, prunes ray hits hidden behind the nearest opaque wall column, skips unnecessary stamp/coverage writes for frontmost opaque columns, writes DOS sprite/gun/HUD runs with direct row fills or incrementing destination pointers instead of per-pixel pitch multiplies, uses DOS mouse polling only while gameplay capture is active, mixes Paula SFX with an integer 22050 Hz Sound Blaster pump that queues directly into SDL's DOS IRQ ring instead of waking the cooperative audio thread, pumps audio between expensive render phases and after present, uses an eight-chunk Sound Blaster ring with a six-chunk fill target for smoother 8 fps pacing, patches the local DOS SDL event and Sound Blaster backends during `tools/build_dos.ps1` so per-frame polling skips the cooperative scheduler yield and only pays mouse INT 33h work while captured, and leaves gameplay update catch-up active.
- DOS display setup now reads `GLOOM.INI` before command-line options, with `resolution=WIDTHxHEIGHT` matching the existing `--resolution`/`--window-size`/`--boot-resolution` limits, plus an optional Amiga-style centered gameplay window from `amiga/gloom2.s` `newwsize`/`largewin`/`calcoffset`: `viewport_size=WIDTHxHEIGHT` renders the gameplay view at reduced chixel dimensions, and `viewport_pixel_size=1x1` or `2x2` scales that centered view while leaving menus and script screens at the selected screen resolution. CLI arguments still override the INI resolution.
- DOS keyboard controls are now configurable through the same `GLOOM.INI` as a PC runtime input shim: `player1_control`/`player2_control` choose keyboard/joystick ownership outside the menu on DOS, and `p1_`/`p2_` action keys such as `p1_forward`, `p1_strafe_left`, `p1_turn_right`, and `p1_fire` replace the previous hardcoded keyboard scancodes before feeding the existing represented `PlayerControls` `joyx`/`joyy`/`joys`/`fire` path. The defaults preserve the earlier PC mappings.
- PC runtime mouse capture and fullscreen handling now stay outside gameplay parity: startup grabs relative mouse input, Escape or P enters a paused gameplay-overlay menu drawn directly over the frozen gameplay frame while clearing active SFX and pausing the SDL audio device, controller Start also opens that pause menu, pause rows can be selected with mouse hover/click, Escape during gameplay releases mouse capture before showing the same gameplay-overlay pause menu, Escape from that menu also releases relative mode/window grab without exiting, web F11 toggles canvas fullscreen through the browser Fullscreen API with resize synchronization, desktop F11 toggles fullscreen-desktop, double-clicking the game window captures again, and mouse fire is suppressed for the recapture click.
- Optional HD presentation art can now be switched on/off at runtime with F10 or the pause menu's HD TEXTURES row when the OpenGL/WebGL renderer is active: walls/flats keep their original Amiga texels plus optional HD buffers, object frames keep original decoded pixels plus optional HD frames, and the OpenGL render-time gate only changes sampled presentation pixels while leaving the original `loadtxts`/`scripttile`/object asset data and gameplay state intact. The maintained software/DOS renderer presents the original source assets even when an HD art root is configured.
- Optional OpenGL/WebGL HD presentation art now also covers original menu/screen artwork and fonts: title/combat/script pictures keep their decoded Amiga IFF pixels plus optional `art/form` or `art/trimmed_pictures` PNG replacements, the menu/HUD fonts keep original decoded glyphs plus optional `art/blit`/`art/blit2`/`art/objects` glyph buffers while preserving the original `fontw`/`fonth`, HUD coordinates, and menu row layout, and partial HD packs fall back to the original SD wall/flat/object/menu/font assets per missing replacement.
- Startup menus now port the represented `dointro`/`startmenu`/`selmenu` title-menu path from `amiga/gloom2.s`: the PC runtime displays the original title image aspect-filled to the render target, decodes the original `misc/bigfont2.bin` menu font, centers rows with the Amiga `printmess2` character mapping, flashes the selected row on the original 13-tick cadence by dimming instead of fully hiding the row, and keeps menu text on exact power-of-two pixel scaling like the HUD. One-player/two-player/exit selections are hooked into the runtime for keyboard, mouse hover/click, and SDL controller D-pad/A/Start/B/Back, with the exit row omitted from the Emscripten web menu; two-player combat, remote link, violence, and about rows are intentionally omitted from the PC main menu for now, while about-text pages remain TODO until their original dependencies are represented. `--skip-menu` is available for deterministic/dev runs.
- PC-only UI feedback sounds now use generated stereo beeps mixed outside the four original Paula-style gameplay SFX channels, so menu movement/back stays a short soft beep and select/activate uses a distinct louder beep without borrowing or starving Amiga gameplay samples.
- Two-player combat flow now ports the represented `initnewgame`/`combatmenu`/`pickcombat`/`scriptplay`/`combatwon` path: selecting combat opens the original combat-series menu, cycles start lives from 2..9, initializes `p1lives`/`p2lives`, randomly selects `com<series>_<map>` using the original `commaps`/`comsleft` shuffle shape, resets health/weapon per combat round, lets player projectiles damage the opposing player, advances through `finished=4` when someone dies, continues to another combat map while both players have lives, and shows the original winner message before returning to the main menu when one player is out.
- Emscripten web build plumbing emits `gloom_pc.html`, uses Emscripten SDL2, preloads the original `amiga/` assets, uses the `package/template.html` shell plus carried-over icons, and exports WebRTC guest hooks so the host can stream the canvas while a guest sends player-two input through the same represented `PlayerControls` fields. Remote WebRTC keyboard/gamepad input always merges into player two and is deliberately independent from the host's local control assignment menu. The Cloudflare Workers/Durable Objects signaling service source and deployment instructions are carried in `webrtc/gloom-signaling/`.
- Desktop/web renderer selection now supports `renderer=auto|opengl|software` in `GLOOM.INI` plus `--renderer`, with auto preferring SDL's OpenGL/WebGL backend outside DOS. The OpenGL/WebGL path now uploads source-backed menu images, menu/HUD fonts, pause backgrounds, and weapon frames as nearest-filtered GPU textures for menus, script/completion screens, pause overlays, and normal gameplay gun/HUD overlays, while DOS remains software-only.
- Desktop/web hardware rendering now has its own `GLOOM.INI` internal presentation target size through `hardware_resolution=WIDTHxHEIGHT`, defaulting to 1920x1080 for `renderer=auto`/`opengl`; the OpenGL/WebGL renderer draws into that target before aspect-fit presentation to the front buffer, while `resolution=WIDTHxHEIGHT` remains the software/DOS framebuffer size and CLI resolution flags override both.
- Desktop/web renderer ownership now has a `RuntimeRenderer` boundary that owns the selected SDL renderer, backend kind, logical framebuffer, sizing, and teardown; the main gameplay render path plus menu restart/autosave flows now take that owner instead of carrying a loose `SDL_Renderer`/`RenderFramebuffer` pair.
- The OpenGL/WebGL gameplay path now renders SD and HD floor/ceiling flats through a WebGL/OpenGL ES 2-style shader using the existing `amiga/gloom2.s` `flat` depth/projection math, 128x128 column-major flat address order, original 16-band depth-darkened palettes for SD assets, repeated HD texture-coordinate scaling, PNG alpha composited over the source flat texel, and `maxz` cutoff before GPU wall, sprite, blood, weapon, HUD, red-palette, and pixelate passes.
- The OpenGL/WebGL path now draws SD and HD walls from original wall texture screens uploaded as RGBA atlases, with the SD atlas still providing alpha only for the Amiga transparent-column palette-index-0 rule from the texture column flag byte. The HD wall atlas packs each loaded `texture_##.png` slot at its own dimensions, so a single original texture screen can mix replacement sizes. The wall shader reuses the existing `wl_sc`/live `textures` remap, door open threshold, `maxz`, and depth-darkening rules, draws column quads far-to-near with discarded original transparent texels, composites HD PNG alpha over the source wall texel instead of creating new wall holes, applies HD wall texels only for loaded HD slots, and feeds current sprite/effects occlusion through an isolated source-derived `drawshapes` mask instead of a software framebuffer presentation pass.
- The OpenGL/WebGL path now draws SD and HD `drawshapes` billboards/effects as GPU column quads whenever GL owns both flats and walls, preserving the existing PC projection, depth sort, source-frame selection, per-column wall/sprite occlusion, transparent palette-index-0 texels, alpha-blended HD PNG edges, and `darktable`-style subtract shading tied to `amiga/gloom2.s` `drawshape_1`, `drawshape_1sc`, `drawshape_8`, `drawshape_q`, and `drawshapes`. HD sprite textures are stored per original `ObjectFrame`, may use different PNG dimensions per billboard/weapon/chunk frame as long as each replacement is at least the original logical frame size, and are sampled through the original logical frame coordinates so optional presentation pixels do not change source handles, scale, frame choice, or gameplay state.
- Optional `hardware_smooth_shading=true` in `GLOOM.INI` now keeps the default OpenGL/WebGL world renderer on the original `amiga/gloom2.s` `initdarktable`/`calcpalettes` 16-band quantized shading, but can opt into continuous hardware presentation shading by interpolating the original SD shade bands and applying continuous HD depth subtract; the desktop/web pause menu exposes the same choice as `SMOOTH SHADING`, while the maintained software/DOS renderer output remains unchanged.
- OpenGL/WebGL raw texture uploads now reset pixel-unpack state before uploading flats, walls, HD presentation atlases, sprites, and post-process source textures, preventing SDL renderer GL state from corrupting HD billboard uploads during `dointro`/`selmenu` to gameplay entry and `scriptplay` level transitions.
- The OpenGL/WebGL path now draws represented `drawblood` one-pixel blood splots as GPU quads when GL owns both flats and walls, preserving the existing PC projection/color path from `amiga/gloom2.s` `drawblood`/`blcols`.
- The OpenGL/WebGL path now applies the represented `redpal`/`palettesr` injury feedback as a GPU post-process after world, sprite/effects overlay, weapon, HUD, and notices are composed. The shader keeps the software transform `red = min((r + g + b + 16*17) / 3, 255)` and supports independent split-screen rectangles for player-one/player-two flashes.
- The OpenGL/WebGL path now applies the repo-local represented pixelate transition as a GPU post-process after red-palette feedback, preserving the existing software block-copy sampling order used for teleport and level-complete pixel-out frames.
- Gameplay frame dumps now support `--frame-dump <bmp>` alongside `--renderer software|opengl`, writing the selected renderer's composed ARGB frame for manual software/OpenGL comparison captures, while diagnostic capture flags can advance represented `doanims`, force the first original `dodoors` door, aim at source-backed door/transparent-wall zones, or load the next `scriptplay` map. `--screen-frame-dump start_menu|combat_menu|pause|script_intro|completion <bmp>` captures deterministic menu/screen frames through the real selected runtime renderer.
- `scripts/compare_renderer_frames.py` now runs software/OpenGL frame dumps for base gameplay, red-palette injury feedback, pixelate, blood, combat, two-player split, animated wall remaps, opening doors, transparent walls, level-transition map loading, start menu, combat menu, pause menu, script intro, and completion scenarios, with scenario-specific bounded pixel-difference metrics for opt-in visual parity checks on machines with an OpenGL-capable SDL video driver.

## In Progress
- Broader runtime object/combat behavior after the renderer/event vertical slice, with projectile-to-enemy damage, pickups/powerups, generic `monsterlogic`/`fire1` enemy projectiles, generic `monsterlogic` movement, baldy/lizard/troll charge-and-punch melee, deathhead cruise/charge movement, special projectile monster routines, dragon movement/fire logic, and cooperative player death/restart logic now in place.
- Remaining audio parity gaps include original music/module playback (`med1`/`med2`) and any gameplay SFX call sites whose owning gameplay routine is not represented yet; soul-suck secondary rendering polish, `exec_teleport` lock/`locklogic`, full intermission script command playback around level transitions, remote link, about pages, and linked/modem combat behavior remain TODO until their original runtime dependencies are ported directly.
- Deterministic parity harness for broader fixed-step player, door, projectile, and event behavior against Amiga-derived expectations.
- Full OpenGL world renderer parity remains TODO: finish moving the remaining lower-level presentation/readback helpers behind the `RuntimeRenderer` boundary, replace the current OpenGL sprite/effects source-derived drawshape mask with a direct GPU-backed occlusion path, and replace any remaining presentation edges that still rely on SDL texture/readback transport where they affect the OpenGL/WebGL path, while preserving the `amiga/gloom2.s` `drawall_`/`drawscene` order and the `doc2p` chunky-present boundary without changing the maintained software/DOS renderer.

## Milestones
1. Bootstrap and Compile
- Root CMakeLists.txt builds executable and fetches SDL2 source.
- App starts window and runs deterministic fixed-step loop.
- Status: complete.

2. Data Ingestion Baseline
- Parse map header, grid block, zones, point table blob, animations, texture name table, and event scripts.
- Validate parser against all files in amiga/maps and amiga/data/maps.
- Status: raw and CrM2-compressed map sweep complete.

3. Vertical Slice Foundation
- Load map1_1 and render debug visualization with player movement/collision scaffold.
- Preserve Amiga-authored simulation rates while allowing the PC runtime to render independently.
- Status: first visible debug build running, improving rendering fidelity now.

## Immediate Next Tasks
1. Finish remaining combat/death parity directly from `gloom2.s`, especially `dragondead` finished-state handling and focused parity tests for projectile/object edge cases.
2. Expand the deterministic replay harness from same-build repeatability into Amiga-derived expectation checks for fixed-step player, door, projectile, and event behavior.
3. Continue audio parity by porting remaining original SFX call sites and `med1`/`med2` music playback without substituting non-Amiga sounds.
4. Expand focused combat parity tests for close barrel-origin hits, enemy removal, wall-hit priority, dragon death chunks, and special-monster projectile cadence.
5. Port remaining script intermission commands around level progression, then implement lock-style teleport event behavior once `locklogic` and the player lock/control state are represented.
